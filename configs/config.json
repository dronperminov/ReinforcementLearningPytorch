{
    "environment": {
        "name": "snake",
        "config": {
            "field_width": 15,
            "field_height": 10
        }
    },

    "algorithm": {
        "name": "a2c",
        "config": {
            "width": 362,
            "plot_keys": ["length"],

            "seed": 42,
            "gamma": 0.9,

            "learning_rate": 0.003,
            "optimizer": "adam",

            "agent_architecture": [
                {"type": "dense", "size": 128, "activation": "relu"},
                {"type": "dense", "size": 128, "activation": "relu"}
            ]
        }
    }
}
